class MyStack:

    def __init__(self):
        self.queue = []
        
    def push(self, x: int) -> None:
        if x not in self.queue:
            self.queue.append(x)

    def pop(self) -> int:
        if len(self.queue) > 0:
            return self.queue.pop()
       
    def top(self) -> int:
        return self.queue[-1]

    def empty(self) -> bool:
        return not self.queue
        
